general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session      # lock while still fully awake, so hyprlock's GPU/EGL context is created on a stable renderer instead of during the flaky post-resume window.
    after_sleep_cmd = hyprctl dispatch dpms on; sleep 1; pidof hyprlock || loginctl lock-session  # wake the screen; if hyprlock died/stalled during sleep (known hyprwm/hyprlock#695), re-lock instead of leaving a dead lockscreen.
    ignore_dbus_inhibit = false                 # respect media playback inhibitors
}

listener {
    timeout = 300                                # 5min.
    on-timeout = brightnessctl -s set 10         # set monitor backlight to minimum, avoid 0 on OLED monitor.
    on-resume = brightnessctl -r                 # monitor backlight restore.
}

listener {
    timeout = 600                                 # 10min (increased from 5min)
    on-timeout = loginctl lock-session            # lock screen when timeout has passed
}

listener {
    timeout = 660                                 # 11min (increased from 5.5min)
    on-timeout = hyprctl dispatch dpms off        # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on          # screen on when activity is detected after timeout has fired.
}

# Suspend only if no audio is playing and no media players are active
listener {
    timeout = 2400                                # 40min (increased from 30min)
    on-timeout = bash -c 'if ! pgrep -f "spotify|firefox|chromium|vlc|mpv|discord|teams" && ! pactl list short sink-inputs | grep -q RUNNING && ! pactl list short source-outputs | grep -q RUNNING; then systemctl suspend; fi'
}
